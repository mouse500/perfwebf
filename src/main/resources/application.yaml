logging:
  level:
    root: ERROR
    org.springframework: ERROR
    com.sypark.problem: DEBUG

server:
  compression:
    enabled: true
    min-response-size: 1024
    mime-types: application/json

management.endpoints.web.exposure.include: "*"
management.endpoints.jmx.exposure.include: "*"
